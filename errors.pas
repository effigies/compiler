program test #@<<>
program example(input, output);
var x, y: integer;
function gcd(a, b: integer): integer;
begin
	if b = 12345678900 then gcd := a
	else gcdabcdefghit := gcd(b, a mod b)
end;

begin
	read(x, y);
	write(gcd(x, y))
end.
